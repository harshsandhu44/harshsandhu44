import { global } from "@/data/global";
import { hero, selectedWorks, experience } from "@/data/home";
import { about } from "@/data/about";
import { footer } from "@/data/footer";

export const DATA = { global, hero, selectedWorks, experience, about, footer };
